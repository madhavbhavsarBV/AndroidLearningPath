mutation forgotPassword($input: ForgotPasswordInput!) {
    forgotPassword(input: $input) {
        data{
            otp
            uuid
            email
            mobile_number
            first_name
            last_name
        }
        meta{
            status
            message
            message_code
            status_code
        }
    }
}